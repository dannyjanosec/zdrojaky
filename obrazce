#include <stdio.h>
int main()
{
    int obrazec = 0;
    int a = 0;
    int b = 0;
    scanf("%d%d%d", &obrazec, &a, &b);
    if (obrazec>=10) {
        printf("Neznamy obrazec\n");
        return 0;
        
    } 
    int middle = a / 2;
    if (obrazec == 0)
    {
        for (int i = 0; i < b; i++)
        { // první je začátek, pak je podmínka a co má udělat, i++ se spustí když vnitřek skončí
            for (int j = 0; j < a; j++)
            {
                printf("x");
            }
            printf("\n");
        }
    }
    if (obrazec == 1)
    {
        for (int i = 0; i < b; i++)
        {
            for (int j = 0; j < a; j++)
            {
                if (i == 0 || i == b - 1 || j == 0 || j == a - 1)
                {
                    printf("x");
                }
                else
                {
                    printf(" ");
                }
            }
            printf("\n");
        }
    }
    if (obrazec == 2)
    {
        int counter = 0;
        for (int i = 0; i < b; i++)
        {
            for (int j = 0; j < a; j++)
            {
                if (i == 0 || i == b - 1 || j == 0 || j == a - 1)
                {
                    printf("x");
                }
                else
                {
                    printf("%d", counter);
                    counter++;
                    if (counter > 9)
                    {
                        counter = 0;
                    }
                }
            }
            printf("\n");
        }
    }
    if (obrazec == 3)
    {
        for (int i = 0; i < a; i++)
        {
            for (int j = 0; j < i; j++)
            {
                printf(" ");
            }
            printf("x");
            printf("\n");
        }
    }
    if (obrazec == 4)
    {
        for (int i = 0; i < a; i++)
        {
            for (int j = 0; j < a - 1 - i; j++)
            {
            
                printf(" ");
            }
            printf("x");
            int j = a-1-i;
            //udělej mezeru tolikrát, kolik jich zbývá do a
            for (j = a-i; j<a; j++){
                printf(" ");
            }
            printf("\n");
        }
    }
    if (obrazec == 5)
     {
        int s = a+a-1;
        int r = s/2;
       for (int i=0; i<a; i++) {
        for (int j=0; j<(a+a-1); j++) {
        if ((i==0 && j==r)) {
            printf("x");
        }
        else if (j== r - i || j== r + i || i==a-1) {
            printf("x");
        }
        else {
            printf(" ") ;
        }
        
        }
         printf("\n");
        }
       
       }
    if (obrazec == 6)
    {
        for (int i = 0; i < b; i++)
        {
            if (i == 0)
            {
                for (int j = 0; j < a; j++)
                {
                    printf("x");
                }
                printf("\n");
            }
            else
            {
                for (int j = 0; j < middle; j++)
                {
                    printf(" ");
                }
                printf("x");
                printf("\n");
            }
        }
    }
    if (obrazec==7) {
    
        for (int i = 0; i < b; i++)
        {
            for (int j = 0; j < a; j++)
            {
                if (i == 0 || i == b/2 || j == 0 || (j == a - 1 && i<b/2))
                {
                    printf("x");
                }
                else
                {
                    printf(" ");
                }
            
            }
           printf("\n");
        }
}
return 0;
}
